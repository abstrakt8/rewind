// Will store the settings after a couple seconds
const storage = window.localStorage;

// export class LocalStorageSettings<Settings> {
//   settings?: Settings;
//
//   constructor(private readonly key: string) {}
//
//   load() {
//     const itemString = storage.getItem(AUDIO_SETT);
//     if (itemString !== null) {
//       this.settings = JSON.parse(itemString);
//     } else {
//       this.settings = DEFAULT_SETTINGS;
//     }
//
//   }
// }
